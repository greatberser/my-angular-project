import { Routes } from '@angular/router';
import { InputPasswordComponent } from '../app/input-password/input-password.component';

export const routes: Routes = [{ path: '', component: InputPasswordComponent }];
